// GET /api/events/:id/participants/:entryId — the entry detail, under the name
// an operator would look for. One implementation; see `participants/index.ts`.

export { onRequestGet } from '../entries/[entryId]';
